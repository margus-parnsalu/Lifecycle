{% macro qsorter(view, name, sort) -%}


    {% do query.update({'sort': sortdir+sort}) %}
    <a href="{{ request.route_url( view, _query=( query )) }}">{{ name }}
            <span class="glyphicon glyphicon-sort"></span></a>

{%- endmacro %}